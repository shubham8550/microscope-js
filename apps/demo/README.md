# @microscope-js/demo

The live demo at https://shubham8550.github.io/microscope-js — a Next.js app statically exported to GitHub Pages by `.github/workflows/pages.yml`.

```bash
pnpm demo            # http://localhost:3000
pnpm demo:build      # static export to apps/demo/out/
```
