# @microscope-js/renderer-docx

Renders `.docx` (and best-effort `.doc`) documents via [mammoth.js](https://github.com/mwilliamson/mammoth.js). All HTML output is run through DOMPurify before being inserted into the DOM.
