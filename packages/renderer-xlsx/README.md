# @microscope-js/renderer-xlsx

Renders `.xlsx`, `.xls`, `.csv`, `.tsv`, `.ods` and other spreadsheet formats via [SheetJS](https://sheetjs.com/). Multiple sheets are exposed as tabs. Formulas are not evaluated and HTML produced by SheetJS is sanitized with DOMPurify.
